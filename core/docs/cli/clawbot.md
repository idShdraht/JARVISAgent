---
summary: "CLI reference for `jarvis clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `jarvis clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `jarvis clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `jarvis clawbot qr` (same behavior as [`jarvis qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `jarvis clawbot qr` -> `jarvis qr`
